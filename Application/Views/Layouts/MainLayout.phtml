<!--
Length of variable's names must be smaller than 30 chars.
Length of section's names must be smaller than 50 chars.
Let don't use two # chars together inside text... If you (will) use it, there (will) appear a problem... :(
-->
#component(defaultComponent, header)

    #section(body)

#component(defaultComponent, footer)